---
phase: 04-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cli/nd.go
  - internal/cli/nd_test.go
  - internal/cli/batch_test.go
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "User sees overlap duration (e.g. 30m) in conflict output"
    - "User sees suggestion to move the later event after the earlier one ends"
    - "Conflict output includes event names and time ranges"
  artifacts:
    - path: "internal/cli/nd.go"
      provides: "Enhanced DetectEventConflicts with overlap duration and suggestion"
      contains: "formatDuration"
    - path: "internal/cli/nd_test.go"
      provides: "Tests asserting overlap duration and suggestion in conflict strings"
      contains: "Suggestion: move"
  key_links:
    - from: "internal/cli/nd.go"
      to: "internal/cli/batch.go"
      via: "collectBatchWarnings calls DetectEventConflicts"
      pattern: "DetectEventConflicts\\(events\\)"
---

<objective>
Enhance conflict detection output so users see exactly how long events overlap and get a suggestion for when to move the later event.

Purpose: UX-03 requires actionable guidance -- the current output says events overlap but not by how much or what to do about it. Neurodivergent users benefit from concrete next steps.
Output: Updated `DetectEventConflicts` function with richer output, `formatDuration` helper, updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ux-polish/04-RESEARCH.md

<interfaces>
<!-- Current signatures and structures the executor needs -->

From internal/cli/nd.go (line 263):
```go
func DetectEventConflicts(events []calendar.Event) []string
```

From internal/cli/batch.go (line 182):
```go
func collectBatchWarnings(events []calendar.Event, opts *batchOptions) []string
```
Call site at batch.go:186:
```go
conflicts := DetectEventConflicts(events)
```

From internal/cli/nd_test.go (line 107-160):
```go
func TestDetectEventConflicts(t *testing.T) {
    now := time.Date(2025, 5, 1, 10, 0, 0, 0, time.UTC)
    // Event 1: 10:00-11:00, Event 2: 10:30-11:30, Event 3: 12:00-13:00
    conflicts := DetectEventConflicts(events)
    // Currently only checks len(conflicts) > 0 -- needs string content assertions
}
```

Test constants from internal/testutil/constants.go:
```go
EventTitleEvent1      = "Event 1"
EventTitleEvent2      = "Event 2"
EventTitleTeamMeeting = "Team Meeting"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enhanced conflict detection with overlap duration and move suggestion</name>
  <files>internal/cli/nd.go, internal/cli/nd_test.go</files>
  <read_first>
    - internal/cli/nd.go (lines 263-288 -- DetectEventConflicts function)
    - internal/cli/nd_test.go (lines 107-160 -- TestDetectEventConflicts)
    - internal/testutil/constants.go (event title constants)
  </read_first>
  <behavior>
    - Test: conflict string for Event 1 (10:00-11:00) and Event 2 (10:30-11:30) contains "by 30m"
    - Test: conflict string contains "Suggestion: move Event 2 to 11:00"
    - Test: no-conflict, empty, single-event cases still return empty slice
    - Test: formatDuration(30*time.Minute) returns "30m"
    - Test: formatDuration(90*time.Minute) returns "1h30m"
    - Test: formatDuration(2*time.Hour) returns "2h"
  </behavior>
  <action>
    1. In nd_test.go, update TestDetectEventConflicts:
       - After `conflicts := DetectEventConflicts(events)` (line 128), add assertion:
         `if !strings.Contains(conflicts[0], "by 30m") { t.Errorf(...) }`
       - Add assertion: `if !strings.Contains(conflicts[0], "Suggestion: move Event 2 to 11:00") { t.Errorf(...) }`
       - Existing len-based checks for no-conflict, empty, single remain unchanged
    2. Add TestFormatDuration with table-driven cases: 30m->"30m", 90m->"1h30m", 2h->"2h", 0->"0m"
    3. Run tests -- they MUST fail (RED)
    4. In nd.go, add unexported `formatDuration(d time.Duration) string`:
       - minutes < 60: return fmt.Sprintf("%dm", minutes)
       - exact hours: return fmt.Sprintf("%dh", hours)
       - else: return fmt.Sprintf("%dh%dm", hours, remaining)
    5. In nd.go, update DetectEventConflicts (line 274-282):
       - After confirming overlap (line 274), calculate:
         overlapStart = max(ev1.StartTime, ev2.StartTime)  -- use if/else, not math.Max
         overlapEnd = min(ev1.EndTime, ev2.EndTime)  -- use if/else
         overlapDuration = overlapEnd.Sub(overlapStart)
         suggestion = ev1.EndTime.Format("15:04")
       - Change format string to:
         `"%s (%s-%s) overlaps with %s (%s-%s) by %s. Suggestion: move %s to %s"`
         with formatDuration(overlapDuration) and suggestion
    6. Run tests -- they MUST pass (GREEN)
  </action>
  <verify>
    <automated>cd /home/malpanez/repos/tempus && go test ./internal/cli/ -run "TestDetectEventConflicts|TestFormatDuration" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "formatDuration" internal/cli/nd.go returns 0
    - grep -q "by %s. Suggestion: move" internal/cli/nd.go returns 0
    - grep -q "by 30m" internal/cli/nd_test.go returns 0
    - grep -q "Suggestion: move Event 2 to 11:00" internal/cli/nd_test.go returns 0
    - grep -q "TestFormatDuration" internal/cli/nd_test.go returns 0
  </acceptance_criteria>
  <done>
    DetectEventConflicts returns strings with overlap duration and move suggestion.
    formatDuration helper converts durations to human-readable strings.
    All existing conflict test cases still pass, new assertions verify enhanced output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update batch warning tests for new conflict format and run full suite</name>
  <files>internal/cli/batch_test.go</files>
  <read_first>
    - internal/cli/batch_test.go (search for "collectBatchWarnings" or "conflict" test cases)
    - internal/cli/batch.go (lines 182-206 -- collectBatchWarnings)
  </read_first>
  <action>
    1. In batch_test.go, find any test that asserts on the exact conflict string format from collectBatchWarnings or DetectEventConflicts output. Update those assertions to match the new format that includes "by Xm. Suggestion: move".
    2. If tests only check `len(conflicts) > 0` or `len(warnings) > 0`, they should already pass -- verify and leave unchanged.
    3. Run the full test suite: `go test ./... -count=1`
    4. Check coverage: `go test ./... -coverprofile=cover.out && go tool cover -func=cover.out | tail -1` -- must be >= 79%.
  </action>
  <verify>
    <automated>cd /home/malpanez/repos/tempus && go test ./... -count=1 && go test ./... -coverprofile=/tmp/cover04.out && go tool cover -func=/tmp/cover04.out | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - go test ./... exits 0
    - Coverage output shows >= 79.0%
  </acceptance_criteria>
  <done>
    All tests pass including batch warning tests with new conflict format. Coverage >= 79%.
  </done>
</task>

</tasks>

<verification>
1. `go test ./internal/cli/ -run "TestDetectEventConflicts|TestFormatDuration" -v` passes
2. `go test ./... -count=1` passes (no regressions)
3. Coverage >= 79%
</verification>

<success_criteria>
- Conflict output includes overlap duration in human-readable format (e.g. "30m", "1h30m")
- Conflict output includes suggestion to move the later event (e.g. "Suggestion: move Event 2 to 11:00")
- No changes to function signatures that would break callers (DetectEventConflicts still returns []string)
- All tests pass, coverage >= 79%
</success_criteria>

<output>
After completion, create `.planning/phases/04-ux-polish/04-01-SUMMARY.md`
</output>
