---
phase: 03-interactive-mode-cli-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cli/app.go
  - internal/cli/app_test.go
  - internal/cli/helpers.go
  - internal/parsing/parsing.go
  - internal/parsing/parsing_test.go
autonomous: true
requirements: [REF-01]

must_haves:
  truths:
    - "App struct exists with Config, Translator, Stdout, Stderr fields"
    - "PersistentPreRunE helper loads config, creates translator, wires App"
    - "All 17 parsing functions are available via internal/parsing package"
    - "Parse(ParseOptions) is the single exported entry point for date/time parsing"
    - "testApp() helper exists for constructing test App instances"
  artifacts:
    - path: "internal/cli/app.go"
      provides: "App struct and NewRootCmd wiring helper"
      exports: ["App", "SetupPersistentPreRunE"]
    - path: "internal/cli/helpers.go"
      provides: "Shared helper functions used across commands"
      exports: ["ParseBoolish", "Slugify", "SplitDelimited", "ExtractDate", "EnsureICSExtension", "EnsureDirForFile", "EnsureUniquePath", "ValueAsString", "ValueAsBool", "ValueAsStringSlice", "ValueAsAlarmSlice", "PrintOK", "PrintErr", "AtoiSafe", "FirstNonEmpty"]
    - path: "internal/parsing/parsing.go"
      provides: "Unified date/time parsing"
      exports: ["Parse", "ParseOptions", "ParseResult"]
    - path: "internal/parsing/parsing_test.go"
      provides: "Tests for all parsing functions"
      contains: "func TestParse"
    - path: "internal/cli/app_test.go"
      provides: "App struct and helper tests"
      contains: "func TestApp"
  key_links:
    - from: "internal/cli/app.go"
      to: "internal/config"
      via: "config.Load() in PersistentPreRunE"
      pattern: "config\\.Load\\(\\)"
    - from: "internal/cli/app.go"
      to: "internal/i18n"
      via: "i18n.NewTranslator in PersistentPreRunE"
      pattern: "i18n\\.NewTranslator"
    - from: "internal/parsing/parsing.go"
      to: "time"
      via: "time.Parse for date/time conversion"
      pattern: "time\\.Parse"
---

<objective>
Create the foundation packages that all subsequent monolith split plans depend on: `internal/cli/` with the App struct and dependency injection wiring, `internal/cli/helpers.go` with shared utility functions, and `internal/parsing/` with the unified date/time parsing API.

Purpose: Establish contracts (types, interfaces, exports) before any command code moves. Downstream plans import these packages without needing to define them.
Output: `internal/cli/app.go`, `internal/cli/app_test.go`, `internal/cli/helpers.go`, `internal/parsing/parsing.go`, `internal/parsing/parsing_test.go`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-interactive-mode-cli-structure/03-CONTEXT.md
@.planning/phases/03-interactive-mode-cli-structure/03-RESEARCH.md
@main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create App struct, helpers, and PersistentPreRunE wiring in internal/cli/</name>
  <files>internal/cli/app.go, internal/cli/app_test.go, internal/cli/helpers.go</files>
  <read_first>
    - main.go (current monolith — read lines 37-80 for var declarations, newRootCmd, PersistentPreRunE pattern; lines 2473-2620 for helper functions: ensureDirForFile, extractDate, parseBoolish, splitDelimited, valueAsString, valueAsBool, valueAsStringSlice, valueAsAlarmSlice, slugify, firstNonEmpty, atoiSafe, printOK, printErr, ensureICSExtension, ensureUniquePath)
    - internal/config/config.go (Config struct, Load(), DefaultConfig(), DetectTimezone(), DetectLanguage())
    - internal/i18n/i18n.go (NewTranslator signature and Translator type)
    - .planning/phases/03-interactive-mode-cli-structure/03-RESEARCH.md (Pattern 1: App Struct with Closure Injection)
  </read_first>
  <action>
    Create `internal/cli/` directory and two files:

    **internal/cli/app.go** (per D-06):
    ```go
    package cli

    type App struct {
        Config     *config.Config
        Translator *i18n.Translator
        Stdout     io.Writer
        Stderr     io.Writer
    }
    ```
    - Import `tempus/internal/config`, `tempus/internal/i18n`, `io`
    - Add `func SetupPersistentPreRunE(app *App) func(cmd *cobra.Command, args []string) error` that:
      1. Calls `config.Load()` — on error, falls back to `config.DefaultConfig()` (per Pitfall 2: tolerant of missing config for init/version)
      2. Overrides config from flags: check `cmd.Flags().GetString("language")` and `cmd.Flags().GetString("timezone")` — only override if non-empty
      3. Sets `app.Config = cfg`
      4. Sets `app.Translator = i18n.NewTranslator(cfg.Language)`
      5. Returns nil
    - Add `func TestApp() *App` (exported for use in test files) that returns an App with `config.DefaultConfig()`, `i18n.NewTranslator("en")`, and `&bytes.Buffer{}` for Stdout/Stderr

    **internal/cli/helpers.go**:
    Move these utility functions from main.go. Export them by capitalizing first letter:
    - `parseBoolish` -> `ParseBoolish(s string) bool` (main.go L2489)
    - `splitDelimited` -> `SplitDelimited(s string) []string` (main.go L2498)
    - `extractDate` -> `ExtractDate(s string) string` (main.go L2481)
    - `ensureICSExtension` -> `EnsureICSExtension(name string) string` (main.go L3470)
    - `ensureDirForFile` -> `EnsureDirForFile(path string) error` (main.go L2473)
    - `ensureUniquePath` -> `EnsureUniquePath(path string) string` (main.go L3481)
    - `valueAsString` -> `ValueAsString(v interface{}) string` (main.go L2515)
    - `valueAsBool` -> `ValueAsBool(v interface{}) bool` (main.go L2535)
    - `valueAsStringSlice` -> `ValueAsStringSlice(v interface{}) []string` (main.go L2550)
    - `valueAsAlarmSlice` -> `ValueAsAlarmSlice(v interface{}) []string` (main.go L2584)
    - `slugify` -> `Slugify(s string) string` (main.go L3716)
    - `firstNonEmpty` -> `FirstNonEmpty(vals ...string) string` (main.go L3686)
    - `atoiSafe` -> `AtoiSafe(s string) int` (main.go L4210)
    - `printOK` -> `PrintOK(w io.Writer, format string, a ...interface{})` — takes writer parameter instead of using package-level stdout (per D-06)
    - `printErr` -> `PrintErr(w io.Writer, format string, a ...interface{})` — takes writer parameter
    - `cleanDisplay` -> `CleanDisplay(s string) string` (main.go L4017)
    - `looksLikeClock` -> `LooksLikeClock(s string) bool` (main.go L3783)
    - `prependToday` -> `PrependToday(clock, tz string) string` (main.go L3787)
    - `fmtDurationHuman` -> `FmtDurationHuman(d time.Duration) string` (main.go L3927)
    - `splitDateTime` -> `SplitDateTime(s string) (string, string)` (main.go L3898)
    Copy the function bodies exactly — do NOT change logic. Only change: capitalize name, add `w io.Writer` parameter to PrintOK/PrintErr.

    Keep the originals in main.go as thin wrappers that delegate to the new exports (e.g., `func parseBoolish(s string) bool { return cli.ParseBoolish(s) }`). This ensures all existing tests still compile and pass. The wrappers will be removed in Plans 02/03 when command code moves.

    **internal/cli/app_test.go**:
    - `package cli` (same package, access unexported symbols)
    - `func TestTestApp(t *testing.T)` — verifies TestApp() returns non-nil Config, Translator, Stdout, Stderr
    - `func TestSetupPersistentPreRunE(t *testing.T)` — creates a cobra.Command with PersistentPreRunE from SetupPersistentPreRunE, runs it, verifies app.Config and app.Translator are non-nil after execution
  </action>
  <verify>
    <automated>cd /home/malpanez/repos/tempus && go test ./internal/cli/ -run "TestTestApp|TestSetupPersistentPreRunE" -count=1 -v && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/cli/app.go` exists and contains `type App struct` with fields `Config *config.Config`, `Translator *i18n.Translator`, `Stdout io.Writer`, `Stderr io.Writer`
    - File `internal/cli/app.go` contains `func SetupPersistentPreRunE(app *App)` that calls `config.Load()` and `i18n.NewTranslator`
    - File `internal/cli/app.go` contains `func TestApp() *App` returning App with DefaultConfig and bytes.Buffer
    - File `internal/cli/helpers.go` exists with `package cli` and exports: `ParseBoolish`, `SplitDelimited`, `ExtractDate`, `EnsureICSExtension`, `EnsureDirForFile`, `EnsureUniquePath`, `ValueAsString`, `ValueAsBool`, `ValueAsStringSlice`, `ValueAsAlarmSlice`, `Slugify`, `FirstNonEmpty`, `AtoiSafe`, `PrintOK`, `PrintErr`
    - `PrintOK` and `PrintErr` in helpers.go accept `io.Writer` as first parameter
    - main.go helper functions are replaced with thin wrappers calling `cli.Xxx()`
    - `go test ./... -count=1` passes (all existing tests still pass)
    - `go vet ./...` passes
  </acceptance_criteria>
  <done>App struct with DI wiring, exported helpers, and testApp() exist in internal/cli/. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create internal/parsing/ package with unified Parse(ParseOptions) entry point</name>
  <files>internal/parsing/parsing.go, internal/parsing/parsing_test.go</files>
  <read_first>
    - main.go lines 479-574 (normalizeTimeInput, parseCreateTimes, parseAllDayTimes, parseTimedEventTimes, parseEndTime, parseDurationEnd)
    - main.go lines 1206-1300 (parseBatchTimes, parseBatchAllDayTimes, parseBatchTimedEventTimes, parseBatchEndTime, parseBatchExplicitEnd, parseBatchDurationEnd)
    - main.go lines 1379-1455 (normalizeAndSpellCheck, normalizeDateTimeInput)
    - main.go lines 3792-3960 (normalizeClockOnlyDateTimes, normalizeEndTimeFromDuration, parseDateTimeWithTZ, parseExDateValues, and all helper functions between)
    - .planning/phases/03-interactive-mode-cli-structure/03-RESEARCH.md (Pattern 3: Parsing Unification, ParseOptions struct)
    - internal/cli/helpers.go (the helpers created in Task 1 that parsing might need)
  </read_first>
  <action>
    Create `internal/parsing/` directory with two files:

    **internal/parsing/parsing.go** (per D-07):
    ```go
    package parsing

    type ParseOptions struct {
        StartDate  string
        StartTime  string
        EndDate    string
        EndTime    string
        Duration   string
        Timezone   string
        EndTZ      string
        AllDay     bool
        Summary    string  // for error messages in batch context
    }

    type ParseResult struct {
        Start time.Time
        End   time.Time
    }

    func Parse(opts ParseOptions) (ParseResult, error) {
        // Normalize inputs first
        // Delegate to parseAllDay or parseTimed
    }
    ```

    Move ALL 17 parsing functions from main.go into this file as unexported helpers:
    1. `normalizeTimeInput` (L479) -> `normalizeTimeInput`
    2. `parseCreateTimes` — refactored INTO `Parse()` (the create-path logic)
    3. `parseAllDayTimes` (L493) -> `parseAllDay`
    4. `parseTimedEventTimes` (L521) -> `parseTimed`
    5. `parseEndTime` (L548) -> `parseEndTime`
    6. `parseDurationEnd` (L564) -> `parseDurationEnd`
    7. `parseBatchTimes` — refactored INTO `Parse()` (the batch-path logic, unified with create-path)
    8. `parseBatchAllDayTimes` (L1213) -> merged with `parseAllDay`
    9. `parseBatchTimedEventTimes` (L1237) -> merged with `parseTimed`
    10. `parseBatchEndTime` (L1258) -> merged with `parseEndTime`
    11. `parseBatchExplicitEnd` (L1271) -> `parseExplicitEnd`
    12. `parseBatchDurationEnd` (L1290) -> merged with `parseDurationEnd`
    13. `normalizeDateTimeInput` (L1409) -> `NormalizeDateTimeInput` (exported — used by normalizer too)
    14. `normalizeClockOnlyDateTimes` (L3792) -> `NormalizeClockOnlyDateTimes` (exported — used by template commands)
    15. `normalizeEndTimeFromDuration` (L3815) -> `NormalizeEndTimeFromDuration` (exported — used by template commands)
    16. `parseDateTimeWithTZ` (L3910) -> `ParseDateTimeWithTZ` (exported — used by template commands)
    17. `parseExDateValues` (L3943) -> `ParseExDateValues` (exported — used by event building)

    Also move these supporting functions used by parsing:
    - `addDurationToStart` (L3883) -> `AddDurationToStart` (exported)
    - `getValueIfKeyNotEmpty` (L3839) -> `GetValueIfKeyNotEmpty` (exported)
    - `trySetEndFromDurationInEnd` (L3846) -> `TrySetEndFromDurationInEnd` (exported)
    - `setEndFromDuration` (L3862) -> `SetEndFromDuration` (exported)
    - `setEndAndDuration` (L3874) -> `SetEndAndDuration` (exported)

    The key unification: `Parse(opts ParseOptions)` replaces both `parseCreateTimes` and `parseBatchTimes`. Inside Parse():
    1. Call `NormalizeDateTimeInput` on StartDate, StartTime, EndDate, EndTime
    2. Call `normalizeTimeInput` if Timezone/EndTZ provided
    3. If AllDay: delegate to `parseAllDay`
    4. If not AllDay: delegate to `parseTimed`

    Keep originals in main.go as thin wrappers that delegate to `parsing.Xxx()`. Wrappers will be removed in Plans 02/03.

    The `var clockOnlyRe` regex (main.go L41) moves to `internal/parsing/parsing.go` as a package-level var.

    **internal/parsing/parsing_test.go**:
    - `package parsing` (same package to test unexported helpers)
    - Move and adapt these tests from main_test.go and main_coverage_test.go:
      - `TestNormalizeDateTimeInput` (main_test.go L2126)
      - `TestParseTimedEventTimesNormalization` (main_test.go L2153)
      - `TestParseAllDayTimesNormalization` (main_test.go L2183)
      - `TestParseDateTimeWithTZ` (main_test.go L1668)
      - `TestAddDurationToStart` (main_test.go L1694)
      - `TestParseExDateValues` (main_test.go L1719)
      - `TestNormalizeClockOnlyDateTimes` (main_test.go L1752)
      - `TestNormalizeEndTimeFromDuration` (main_test.go L1821)
      - `TestParseEndTime` (main_coverage_test.go L333)
      - `TestParseDurationEnd` (main_coverage_test.go L403)
      - `TestLooksLikeClock` (main_test.go L1149) — NOTE: looksLikeClock moved to helpers.go in Task 1; keep test in main_test.go using wrapper, or move here if it's parsing-related. Since it's used by normalizeClockOnlyDateTimes, move test here as `TestLooksLikeClock` calling `cli.LooksLikeClock`.
    - Add new `TestParse` that tests the unified entry point with:
      - All-day event: `Parse(ParseOptions{StartDate: "2025-01-15", AllDay: true})` -> valid start, end = start+24h
      - Timed event: `Parse(ParseOptions{StartDate: "2025-01-15", StartTime: "10:00", Duration: "1h"})` -> valid start, end = start+1h
      - Timed with end time: `Parse(ParseOptions{StartDate: "2025-01-15", StartTime: "10:00", EndTime: "11:30"})` -> correct end
      - Slash dates: `Parse(ParseOptions{StartDate: "2025/01/15", StartTime: "10:00", Duration: "1h"})` -> normalized, valid
      - Missing required fields: `Parse(ParseOptions{})` -> error
    - Import `tempus/internal/cli` for `LooksLikeClock` only if needed by tests; otherwise keep parsing tests self-contained.
  </action>
  <verify>
    <automated>cd /home/malpanez/repos/tempus && go test ./internal/parsing/ -count=1 -v && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/parsing/parsing.go` exists with `package parsing`
    - Contains `type ParseOptions struct` with fields: StartDate, StartTime, EndDate, EndTime, Duration, Timezone, EndTZ, AllDay, Summary
    - Contains `type ParseResult struct` with fields: Start, End (both time.Time)
    - Contains `func Parse(opts ParseOptions) (ParseResult, error)` as the exported entry point
    - Contains exported functions: `NormalizeDateTimeInput`, `NormalizeClockOnlyDateTimes`, `NormalizeEndTimeFromDuration`, `ParseDateTimeWithTZ`, `ParseExDateValues`
    - `internal/parsing/parsing_test.go` contains `func TestParse` with at least 5 subtests
    - `go test ./internal/parsing/ -count=1` passes
    - `go test ./... -count=1` passes (all existing tests still compile via wrappers in main.go)
    - `go vet ./...` passes
  </acceptance_criteria>
  <done>internal/parsing/ package exists with unified Parse() entry point. All 17 parsing functions migrated. All tests pass including new TestParse and migrated parsing tests.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `go test ./... -count=1` — all tests pass
2. `go vet ./...` — no issues
3. `go test ./... -coverprofile=coverage.out && go tool cover -func=coverage.out | tail -1` — coverage >= 79%
4. `grep "type App struct" internal/cli/app.go` — confirms App struct exists
5. `grep "func Parse(" internal/parsing/parsing.go` — confirms unified entry point
</verification>

<success_criteria>
- internal/cli/app.go, internal/cli/helpers.go, internal/cli/app_test.go exist with correct exports
- internal/parsing/parsing.go, internal/parsing/parsing_test.go exist with unified Parse() API
- All existing tests pass without modification (thin wrappers in main.go delegate to new packages)
- Coverage remains >= 79%
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-mode-cli-structure/03-01-SUMMARY.md`
</output>
