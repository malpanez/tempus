---
phase: 01-bug-fixes-test-infrastructure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config_test.go
autonomous: true
gap_closure: true
requirements: [BUG-01, BUG-02, BUG-03, BUG-04, BUG-05, REF-06, REF-02]

must_haves:
  truths:
    - "Total test coverage is >= 79.0%"
    - "GetAlarmProfile returns correct triggers for known profiles and nil for unknown"
    - "ListAlarmProfiles returns all profile names from config"
  artifacts:
    - path: "internal/config/config_test.go"
      provides: "Tests for GetAlarmProfile and ListAlarmProfiles"
      contains: "TestGetAlarmProfile"
    - path: "internal/config/config_test.go"
      provides: "Tests for ListAlarmProfiles"
      contains: "TestListAlarmProfiles"
  key_links:
    - from: "internal/config/config_test.go"
      to: "internal/config/config.go"
      via: "direct method calls on Config struct"
      pattern: "cfg\\.GetAlarmProfile\\|cfg\\.ListAlarmProfiles"
---

<objective>
Close the coverage gap (78.7% -> >= 79.0%) by adding unit tests for two untested config helper functions: `GetAlarmProfile` and `ListAlarmProfiles`.

Purpose: The 79% coverage gate is a phase success criterion. Phase 01 improved coverage from 78.2% to 78.7% but fell 0.3pp short. These two small functions at 0% coverage are the lowest-effort path to cross the threshold.
Output: Updated `internal/config/config_test.go` with new test functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bug-fixes-test-infrastructure/01-VERIFICATION.md

@internal/config/config.go
@internal/config/config_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for GetAlarmProfile and ListAlarmProfiles</name>
  <files>internal/config/config_test.go</files>

  <read_first>
    - internal/config/config.go (lines 219-240 for the two target functions, lines 27-42 for defaultConfig with AlarmProfiles)
    - internal/config/config_test.go (full file for existing patterns and imports)
  </read_first>

  <action>
Add two test functions to `internal/config/config_test.go` following the existing table-driven test pattern used in that file.

**TestGetAlarmProfile** — test cases:
1. Known profile "adhd-default" returns `[]string{"-2h", "-1h", "-30m", "-10m"}`
2. Known profile "medication" returns `[]string{"-5m", "-1m", "0m"}`
3. Known profile "none" returns empty slice `[]string{}`
4. Unknown profile "nonexistent" returns nil
5. Config with nil AlarmProfiles map returns nil

For case 5, create a `Config{}` (zero value) where `AlarmProfiles` is nil, then call `GetAlarmProfile("anything")` and assert nil return.

**TestListAlarmProfiles** — test cases:
1. Default config returns all 5 profile names ("adhd-default", "adhd-countdown", "medication", "single", "none") — use `len()` check (5) plus a set-membership check since map iteration order is non-deterministic
2. Config with nil AlarmProfiles returns empty slice (not nil)
3. Config with single profile returns slice of length 1

For these tests, do NOT use `Load()` (which touches filesystem/viper). Instead, construct `Config` structs directly with the `AlarmProfiles` field set. This avoids filesystem side effects and makes tests fast and isolated.

Pattern to follow:
```go
func TestGetAlarmProfile(t *testing.T) {
    cfg := &Config{
        AlarmProfiles: map[string][]string{
            "adhd-default": {"-2h", "-1h", "-30m", "-10m"},
            "medication":   {"-5m", "-1m", "0m"},
            "none":         {},
        },
    }
    // table-driven subtests...
}
```
  </action>

  <verify>
    <automated>cd /home/malpanez/repos/tempus && /usr/local/go/bin/go test ./internal/config/ -run "TestGetAlarmProfile|TestListAlarmProfiles" -v -count=1</automated>
  </verify>

  <acceptance_criteria>
    - grep -q "func TestGetAlarmProfile" internal/config/config_test.go
    - grep -q "func TestListAlarmProfiles" internal/config/config_test.go
    - go test ./internal/config/ -run "TestGetAlarmProfile|TestListAlarmProfiles" -v exits 0
    - go test ./internal/config/ -count=1 exits 0 (no regressions)
  </acceptance_criteria>

  <done>
    TestGetAlarmProfile passes with 5 subtests (known profiles return correct triggers, unknown returns nil, nil map returns nil).
    TestListAlarmProfiles passes with 3 subtests (default profiles listed, nil map returns empty slice, single-profile config works).
    All existing config tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify coverage gate crossed</name>
  <files>internal/config/config_test.go</files>

  <read_first>
    - internal/config/config_test.go (confirm new tests exist from Task 1)
  </read_first>

  <action>
Run the full test suite with coverage profiling and verify total coverage is >= 79.0%.

1. Run: `go test ./... -coverprofile=cover.out -count=1`
2. Run: `go tool cover -func=cover.out | grep total`
3. Verify the total percentage is >= 79.0%
4. Also check internal/config coverage specifically: `go tool cover -func=cover.out | grep -A2 "GetAlarmProfile\|ListAlarmProfiles"` to confirm those functions now show coverage > 0%
5. Clean up: `rm cover.out`

If coverage is still below 79.0%, identify the next smallest uncovered function in internal/config/config.go and add a test for it. The `LoadFromFile` with alarm_profiles in YAML config would be a good candidate (extends TestLoadFromFile to include alarm_profiles field).
  </action>

  <verify>
    <automated>cd /home/malpanez/repos/tempus && /usr/local/go/bin/go test ./... -coverprofile=cover.out -count=1 && /usr/local/go/bin/go tool cover -func=cover.out | grep total && rm cover.out</automated>
  </verify>

  <acceptance_criteria>
    - go test ./... -count=1 exits 0 (all 1435+ tests pass)
    - go tool cover total line shows >= 79.0%
    - go tool cover shows GetAlarmProfile coverage > 0%
    - go tool cover shows ListAlarmProfiles coverage > 0%
  </acceptance_criteria>

  <done>
    Full test suite passes. Total coverage >= 79.0%. Coverage gate closed.
  </done>
</task>

</tasks>

<verification>
1. `go test ./internal/config/ -v -count=1` — all config tests pass including new ones
2. `go test ./... -count=1` — full suite passes, no regressions
3. `go test ./... -coverprofile=cover.out && go tool cover -func=cover.out | grep total` — shows >= 79.0%
</verification>

<success_criteria>
- Total project test coverage >= 79.0% (was 78.7%)
- GetAlarmProfile and ListAlarmProfiles have > 0% coverage (was 0%)
- All existing tests continue to pass (1435+ tests)
- No implementation code changed — tests only
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug-fixes-test-infrastructure/01-04-SUMMARY.md`
</output>
